% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_text.R
\name{classify_text}
\alias{classify_text}
\title{Classify the language of a text}
\usage{
classify_text(text, model = NULL, threshold = 0, details = FALSE, ...)
}
\arguments{
\item{text}{Character vector. One or more texts to classify.}

\item{model}{A trained \code{textmodel_nb} object (from
\code{\link[=build_classifier]{build_classifier()}}) or the default classifier. If \code{NULL}
(default), the classifier shipped with the package is used.}

\item{threshold}{Numeric scalar in \verb{[0, 1]}. If the probability of the
best-scoring language is below this value, the text is considered
unidentifiable and the prediction is returned as \code{NA}.}

\item{details}{Logical. If \code{TRUE}, returns a data frame with a row per
text and a column per language holding the posterior probabilities, plus
a \code{label} column (the best-scoring language, or \code{NA} when its
probability is below \code{threshold}) and a \code{probability} column (the raw
probability of that best-scoring language). If \code{FALSE} (default),
returns a named character vector with the best language per text (or
\code{NA} when below \code{threshold}).}

\item{...}{Additional arguments passed to
\code{\link[quanteda.textmodels:predict.textmodel_nb]{quanteda.textmodels::predict.textmodel_nb()}}.}
}
\value{
Either a named character vector of predicted language codes, or
(when \code{details = TRUE}) a data frame with the full probability table.
}
\description{
Predicts the language of one or more texts using a Naive Bayes
classifier trained on stopword lists (see \code{\link[=build_classifier]{build_classifier()}}). By
default the package ships a pre-trained classifier covering all
languages from the \code{stopwords} package, so you can classify texts
without building a model first.
}
\examples{
classify_text("Olá pessoal, meu nome é João")
classify_text(c("Hello everyone, my name is John",
                "Ciao a tutti, il mio nome è Marco"))
classify_text("Hola a todos, mi nombre es João", details = TRUE)
}
\seealso{
\code{\link[=build_classifier]{build_classifier()}}, \code{\link[=available_languages]{available_languages()}}
}
