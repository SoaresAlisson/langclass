% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_classifier.R
\name{build_classifier}
\alias{build_classifier}
\title{Build a Naive Bayes language classifier}
\usage{
build_classifier(
  source = "stopwords-iso",
  prior = c("uniform", "docfreq", "termfreq"),
  ...
)
}
\arguments{
\item{source}{Character; passed to \code{\link[=available_languages]{available_languages()}}. Defaults to
\code{"stopwords-iso"}.}

\item{prior}{Character; the prior distribution used by
\code{\link[quanteda.textmodels:textmodel_nb]{quanteda.textmodels::textmodel_nb()}}. One of \code{"uniform"},
\code{"docfreq"} or \code{"termfreq"}. Defaults to \code{"uniform"}.}

\item{...}{Additional arguments passed to
\code{\link[quanteda.textmodels:textmodel_nb]{quanteda.textmodels::textmodel_nb()}}.}
}
\value{
A trained \code{textmodel_nb} object, suitable for passing to
\code{\link[=classify_text]{classify_text()}}.
}
\description{
Trains a Naive Bayes model over the stopword lists for every language
reported by \code{\link[=available_languages]{available_languages()}} using
\code{\link[quanteda.textmodels:textmodel_nb]{quanteda.textmodels::textmodel_nb()}}. Each language contributes one
"document" containing its stopwords, and the class label is the language
code. The resulting model can classify any new text into one of those
languages.
}
\examples{
\dontrun{
m <- build_classifier()
classify_text("Hallo Leute, mein Name ist Johannes", model = m)
}
}
\seealso{
\code{\link[=available_languages]{available_languages()}}, \code{\link[=classify_text]{classify_text()}}
}
